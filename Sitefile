
task :default => :build

desc 'deploy the site to the webserver'
task :deploy => [:build, 'deploy:rsync']

SITE.create_mode = 'directory'
SITE.tidy_options = '-indent -wrap 180  -utf8' # -access 3'

SITE.user = 'deploy'
SITE.host = 'hendrikvolkmer.de'
SITE.remote_dir = '/srv/hendrikvolkmer.de/blog-new/public'
#SITE.rsync_args = ['--delete -r']
