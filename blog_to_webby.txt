for FILE in * 
do
  YEAR=$( echo $FILE | cut -f1 -d- )  
  MONTH=$( echo $FILE | cut -f2 -d- )
  DAY=$( echo $FILE | cut -f3 -d- )
  SLUG=$( echo $FILE | cut -f4-20 -d- )
  DIR="$YEAR/$MONTH/$DAY/$(basename $SLUG .markdown)"
  mkdir -p $DIR
  mv $FILE ${DIR}/index.txt
done